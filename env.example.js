// ── ENVIRONMENT CONFIGURATION ─────────────────────────────────
// Copy this file, rename it to env.js, and fill in your values.
// NEVER commit env.js to git — it contains secret keys!
//
// env.js is already listed in .gitignore for your safety.

window.ENV = { 
    N8N_WEBHOOK_URL: ''   // Your n8n production webhook URL here
};
